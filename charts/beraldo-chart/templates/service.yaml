apiVersion: v1
kind: Service
metadata:
  name: {{ include "beraldo-chart.fullname" . }}
  labels:
    {{- include "beraldo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beraldo-chart.selectorLabels" . | nindent 4 }}
