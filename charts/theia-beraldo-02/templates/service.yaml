apiVersion: v1
kind: Service
metadata:
  name: {{ include "theia-beraldo-02.fullname" . }}
  labels:
    {{- include "theia-beraldo-02.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "theia-beraldo-02.selectorLabels" . | nindent 4 }}
